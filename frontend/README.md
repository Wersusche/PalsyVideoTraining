# Palsy Video Training Frontend

This directory contains the React + Vite + Tailwind (shadcn/ui ready) starter.

## Setup
```bash
cd frontend
npm install
npm run dev
```

Run a production build with:
```bash
npm run build
```
